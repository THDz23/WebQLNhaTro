@model IEnumerable<WebQLNhaTro.Models.searchprice>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

@section naviheader{
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/admin/home" class="nav-link">Trang chủ</a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/admin/searchprice" class="nav-link">Phân khúc giá</a>
        </li>
    </ul>
}


<section class="content">

    <!-- Default box -->
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Danh sách phân khúc giá</h3>

            <div class="card-tools">
                <a href="/admin/searchprice/add" class="btn btn-primary">Thêm mới</a>
            </div>
        </div>
        <div class="card-body">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>Mã</th>
                        <th>giá từ</th>
                        <th>Ngày tạo</th>
                        <th>Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var tro in Model)
                    {
                        <tr>
                            <td>@tro.ID</td>
                            <td>@tro.PriceFrom</td>
                            <td>@tro.CreateDate</td>
                            <td>
                                <a href="/admin/searchprice/edit/@tro.ID" class="btn btn-sm btn-primary">Sửa</a>
                                <a href="/admin/searchprice/delete/@tro.ID" class="btn btn-sm btn-danger">Xóa</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>

        </div>
        <!-- /.card-body -->

    </div>
    <!-- /.card -->

</section>



